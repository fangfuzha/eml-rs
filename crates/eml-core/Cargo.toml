[package]
name = "eml-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
description = "no_std core EML primitives and numeric policies."
license = "MIT"

[dependencies]
libm = "0.2"
num-complex = { version = "0.4", default-features = false, features = ["libm"] }
