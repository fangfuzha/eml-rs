# v0.2.0 Snapshot / 版本快照

## 中文

- 发布说明: `release-notes.md`
- 发布前验证清单与记录: `verification.md`
- 本版本不单独维护迁移说明；Stable API 推荐路径保持 `compile()` / `PipelineBuilder` / `CompiledPipeline`，`compile_expression()` 在 `0.2.x` 周期继续保留。
- 本快照把 `paper-basis` 复现资产、研究 artifact、portable graph CLI 导出与性能门禁决策收束到 `v0.2.0` 版本文档。正式 GitHub Release 仍以 `v0.2.0` tag、workflow artifacts 与 release assets 的最终发布为准。

## English

- Release notes: `release-notes.md`
- Pre-release verification checklist and record: `verification.md`
- This version does not maintain a separate migration note. The Stable API path remains `compile()` / `PipelineBuilder` / `CompiledPipeline`, and `compile_expression()` remains available throughout the `0.2.x` cycle.
- This snapshot consolidates `paper-basis` reproduction assets, research artifacts, portable graph CLI export, and performance-gate decisions into the `v0.2.0` version docs. The formal GitHub Release is still defined by the final `v0.2.0` tag, workflow artifacts, and release assets.
